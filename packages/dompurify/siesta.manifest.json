{
  "siesta": "1.0",
  "library": "siesta-dompurify",
  "version": "0.1.0",
  "description": "XSS sanitizer",
  "minSiestaVersion": "1.0",
  "adapter": {
    "class": "DompurifyAdapter"
  },
  "config": {
    "locale": {
      "type": "string",
      "default": "en",
      "description": "Default locale"
    }
  },
  "factories": {
    "create": {
      "returns": "Validator",
      "description": "Create a Validator via dompurify",
      "params": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  "types": {
    "Validator": {
      "methods": {
        "validate": {
          "returns": "boolean",
          "params": {
            "type": "object",
            "properties": {
              "value": {}
            },
            "required": [
              "value"
            ]
          }
        },
        "assert": {
          "returns": "Validator",
          "params": {
            "type": "object",
            "properties": {
              "value": {}
            },
            "required": [
              "value"
            ]
          }
        }
      }
    }
  }
}
