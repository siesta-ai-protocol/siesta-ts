{
  "siesta": "1.0",
  "library": "siesta-joi",
  "version": "0.2.0",
  "description": "Object schema validation (deep experimental surface)",
  "minSiestaVersion": "1.0",
  "adapter": {
    "class": "JoiAdapter"
  },
  "config": {
    "locale": {
      "type": "string",
      "default": "en",
      "description": "Default locale"
    },
    "strict": {
      "type": "boolean",
      "default": true,
      "description": "Strict validation / parsing"
    }
  },
  "factories": {
    "create": {
      "returns": "Validator",
      "description": "Create a Validator via joi",
      "params": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    "parse": {
      "returns": "Validator",
      "description": "Parse / build from input",
      "params": {
        "type": "object",
        "properties": {
          "input": {
            "type": "string",
            "description": "Raw input"
          }
        },
        "required": [
          "input"
        ]
      }
    }
  },
  "types": {
    "Validator": {
      "methods": {
        "validate": {
          "returns": "boolean",
          "params": {
            "type": "object",
            "properties": {
              "value": {}
            },
            "required": [
              "value"
            ]
          }
        },
        "assert": {
          "returns": "Validator",
          "params": {
            "type": "object",
            "properties": {
              "value": {}
            },
            "required": [
              "value"
            ]
          }
        },
        "inspect": {
          "returns": "string",
          "description": "Debug snapshot string",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          }
        },
        "reset": {
          "returns": "Validator",
          "description": "Reset to initial state",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          }
        }
      }
    }
  }
}
