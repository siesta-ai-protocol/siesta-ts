{
  "siesta": "1.0",
  "library": "siesta-crypto-js",
  "version": "0.2.0",
  "description": "Crypto standards (deep experimental surface)",
  "minSiestaVersion": "1.0",
  "adapter": {
    "class": "CryptoJsAdapter"
  },
  "config": {
    "locale": {
      "type": "string",
      "default": "en",
      "description": "Default locale"
    },
    "strict": {
      "type": "boolean",
      "default": true,
      "description": "Strict validation / parsing"
    }
  },
  "factories": {
    "create": {
      "returns": "Crypto",
      "description": "Create a Crypto via crypto-js",
      "params": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    "parse": {
      "returns": "Crypto",
      "description": "Parse / build from input",
      "params": {
        "type": "object",
        "properties": {
          "input": {
            "type": "string",
            "description": "Raw input"
          }
        },
        "required": [
          "input"
        ]
      }
    }
  },
  "types": {
    "Crypto": {
      "methods": {
        "encode": {
          "returns": "string",
          "params": {
            "type": "object",
            "properties": {
              "payload": {
                "type": "string"
              }
            },
            "required": [
              "payload"
            ]
          }
        },
        "decode": {
          "returns": "string",
          "params": {
            "type": "object",
            "properties": {
              "token": {
                "type": "string"
              }
            },
            "required": [
              "token"
            ]
          }
        },
        "inspect": {
          "returns": "string",
          "description": "Debug snapshot string",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          }
        },
        "reset": {
          "returns": "Crypto",
          "description": "Reset to initial state",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          }
        }
      }
    }
  }
}
