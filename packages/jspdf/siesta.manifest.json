{
  "siesta": "1.0",
  "library": "siesta-jspdf",
  "version": "0.2.0",
  "description": "PDF generation in JS (deep experimental surface)",
  "minSiestaVersion": "1.0",
  "adapter": {
    "class": "JspdfAdapter"
  },
  "config": {
    "locale": {
      "type": "string",
      "default": "en",
      "description": "Default locale"
    },
    "strict": {
      "type": "boolean",
      "default": true,
      "description": "Strict validation / parsing"
    }
  },
  "factories": {
    "create": {
      "returns": "Pdf",
      "description": "Create a Pdf via jspdf",
      "params": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    "parse": {
      "returns": "Pdf",
      "description": "Parse / build from input",
      "params": {
        "type": "object",
        "properties": {
          "input": {
            "type": "string",
            "description": "Raw input"
          }
        },
        "required": [
          "input"
        ]
      }
    }
  },
  "types": {
    "Pdf": {
      "methods": {
        "addPage": {
          "returns": "Pdf",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          }
        },
        "save": {
          "returns": "boolean",
          "params": {
            "type": "object",
            "properties": {
              "path": {
                "type": "string"
              }
            },
            "required": [
              "path"
            ]
          }
        },
        "inspect": {
          "returns": "string",
          "description": "Debug snapshot string",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          }
        },
        "reset": {
          "returns": "Pdf",
          "description": "Reset to initial state",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          }
        }
      }
    }
  }
}
