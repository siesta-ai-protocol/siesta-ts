{
  "siesta": "1.0",
  "library": "siesta-sanitize-html",
  "version": "0.2.0",
  "description": "HTML sanitizer (deep experimental surface)",
  "minSiestaVersion": "1.0",
  "adapter": {
    "class": "SanitizeHtmlAdapter"
  },
  "config": {
    "locale": {
      "type": "string",
      "default": "en",
      "description": "Default locale"
    },
    "strict": {
      "type": "boolean",
      "default": true,
      "description": "Strict validation / parsing"
    }
  },
  "factories": {
    "create": {
      "returns": "Validator",
      "description": "Create a Validator via sanitize-html",
      "params": {
        "type": "object",
        "properties": {},
        "required": []
      },
      "since": "1.0.0"
    },
    "parse": {
      "returns": "Validator",
      "description": "Parse / build from input",
      "params": {
        "type": "object",
        "properties": {
          "input": {
            "type": "string",
            "description": "Raw input"
          }
        },
        "required": [
          "input"
        ]
      },
      "since": "1.0.0"
    }
  },
  "types": {
    "Validator": {
      "methods": {
        "validate": {
          "returns": "boolean",
          "params": {
            "type": "object",
            "properties": {
              "value": {}
            },
            "required": [
              "value"
            ]
          },
          "since": "1.0.0"
        },
        "assert": {
          "returns": "Validator",
          "params": {
            "type": "object",
            "properties": {
              "value": {}
            },
            "required": [
              "value"
            ]
          },
          "since": "1.0.0"
        },
        "inspect": {
          "returns": "string",
          "description": "Debug snapshot string",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          },
          "since": "1.0.0"
        },
        "reset": {
          "returns": "Validator",
          "description": "Reset to initial state",
          "params": {
            "type": "object",
            "properties": {},
            "required": []
          },
          "since": "1.0.0"
        }
      }
    }
  },
  "package": {
    "name": "sanitize-html",
    "version": "1.0.0",
    "range": "^1.0 || ^2.0 || ^3.0 || ^4.0"
  }
}
